package xyz.kwahson.core;

/**
 * Kwah's Core: shared utilities for NeoForge 1.21.1 mods.
 * <p>
 * This is a build-time library, not a standalone mod. It gets shaded
 * into each mod's jar so users never see or install it separately.
 */
public final class KwahsCore {
  public static final String VERSION = "0.3.0";

  private KwahsCore() {}
}
