# Kwah's Core -- TODO

## Config

- [ ] `validateOrReset` should accept `ModConfig.Type` to build the correct filename (currently hardcodes `-client.toml`, breaks for COMMON and SERVER configs)
- [ ] Payload helpers: generic empty/single-value payload factories + registration helper

## Backlog

- [ ] Keybind-to-packet handler (if pattern repeats enough across mods)
